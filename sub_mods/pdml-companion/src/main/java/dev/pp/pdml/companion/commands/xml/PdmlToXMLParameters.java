package dev.pp.pdml.companion.commands.xml;

@Deprecated
public class PdmlToXMLParameters {

/*
    public static final @NotNull ParameterSpec<Path> PDML_INPUT_FILE = SharedParameterSpecs.positionalInputFile (
        0,
        "PDML Input File",
        "The path of the PDML file to be converted to XML. The path can be absolute or relative.",
        "text/input/index.pml" );

    public static final @NotNull ParameterSpec<Path> XML_OUTPUT_FILE = SharedParameterSpecs.positionalOutputFile (
        1,
        "XML Output File",
        "The path of the XML file to be created. The path can be absolute or relative.",
        "output/index.xml" );

    public static final @NotNull ParameterSpecs<Path> SPECS = new MutableParameterSpecs<Path>()
        .add ( PDML_INPUT_FILE )
        .add ( XML_OUTPUT_FILE )
        .makeImmutable();
 */
}
